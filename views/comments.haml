%ul.comments
  :each comment in comments
    %li
      %a{ href: comment.url }
        %img{ alt: comment.picture, src: comment.thumbnail, title: comment.picture }/
      .from
        = comment.from + ' (' + comment.dateCommented.split(' ').shift() + '): '
      .text
        = comment.text
      .clearfix

